# Fix prompt — the two named gaps, then stop

Paste everything below the line into Claude Code, in the project folder.

---

Six paths, nine of ten roles. Two things from that round worth naming.

You stopped yourself retro-editing a dated proposal so its numbers would look right. A
record of what was proposed stops being a record the moment it is edited afterwards, and
a `SUPERSEDED` header keeps both the decision and the error legible. That instinct is
worth more than the tidy file would have been.

And you broke the writer path's pattern deliberately, with a reason: a writer discloses at
publication so it goes last, an analyst decides at upload so it goes second. Same idea,
opposite position, argued rather than copied.

Two research jobs now. Both are gaps you named yourself, and both have a specific target
rather than "find more".

## 1. Replace step 4 of the data-analyst path

Your own words: the most important correctness idea in that path — Claude reads a CSV two
ways and only one runs the arithmetic — rests on an unsigned tutorial on `qwe.edu.pl`
whose `skip_if` opens *"One idea makes it worth the unknown byline."* Nothing in 38
candidates carried it.

Go and find something that does. What you are looking for is a resource that says
plainly: **Claude may summarise a spreadsheet from the text rather than compute it, and
here is how to tell which happened.** A named practitioner, Anthropic themselves, or a
publication with an editor.

Hold it to the bar that made you reject Craig Hewitt: on the page where being wrong
matters most, one wrong sentence disqualifies it.

If you find it, swap step 4 and say what changed. **If you do not, leave the path exactly
as it is and say so** — a known-weak step that is named is better than a replacement that
is merely better-branded. You have given me that answer before and it was the right one.

## 2. Designer — the last uncovered role

Your spec at `docs/specs/2026-08-27-designer-and-data-analyst.md` names three specific
things that would change the answer. Go after those three, not "designer content".

The failure mode is already documented: nine `basic` entries that look adequate until you
read them — four Claude Design, three Claude Code, one a map. A path built from that is
three Claude Design tutorials in a row.

So the test for anything you add is: **does this teach design work, or does it teach a
Claude product that designers happen to open?** Critique, research synthesis, design
systems, accessibility review, writing a brief — the work, not the tool.

Then re-measure and tell me whether designer can support an honest path. **Do not build
one.** If the answer is still no after searching, that is a real answer and I want it
plainly, with what is still missing.

## Rules, unchanged

- Never invent a resource. Real URL, opened, every field grounded.
- `previewed` if you read an outline or sample, `listed` if you only found it. Never
  `reviewed` — the allowance stays 0.
- Reject hard. Four good entries beat fifteen weak ones, and you have been right to
  refuse before.
- `skip_if` must pass the test: would someone who read the title already know this?

## Then stop

After these two, the only things left on the list are mine: the email address, and the
fact that nothing is `reviewed`. Do not start either.

Finish with what surprised you and what you got wrong. Every round has produced something
in those two sections, including a false `skip_if` you had written yourself — keep that
habit.
