# Fix prompt — the checkpoint passes; finish the harvest with two legs

Paste everything below the line into Claude Code, in the project folder.

---

The ten pass. I read every `skip_if` and opened a URL myself — the March 16 date is
printed on the page, so it is real. Entry 10 is the rule earning its keep. The Help
Center finding is the important one: the beginner scarcity was partly our harvesting,
not the world. Go on — with one correction and one change of shape.

# The correction — titles are the page's own words

Entry 1 is titled *"Claude claims it sent an email or made a file. It did not."* That is
a good sentence and it is not the page's title. A title is what the reader sees;
re-authoring it is the thing this project already wrote down as forbidden, even when
the real one is unwieldy. Rule for the whole harvest: **use the page's own title.
Trim it if you must — cut, never paraphrase — and the parenthetical suffix convention
stands.** Fix entry 1 before merging.

# The change of shape — two legs, not one

All ten are Anthropic, all `docs`, all under 15 minutes. Right for the Help Center
leg. Wrong as the shape of the round: the catalogue is 61% official,
`researcher|never-used` holds zero non-Anthropic items, and every one of these ten
deepens that. The picks need a non-Anthropic item and a second format in every pool,
and they cannot conjure one.

**Leg 1 — the Help Center, finished.** Walk the three sub-sections you named — Get
started, Troubleshooting, Usage and limits — and the Cowork, Desktop and Mobile
collections for `never-used`. Same bar, same method. Skip anything that is a policy
notice rather than learning material; your source list already draws that line.

**Leg 2 — non-Anthropic, at `never-used` first.** Walk indexes, not search results:
the YouTube channels in `research/reports/youtube-*.json`, the publishers in the
August research files, the inbox reports. Prefer **video and course** — a person who
has never used Claude often wants to watch someone use it, and constraint 3 needs
the format. Every entry opened, every date real or `UNVERIFIED`, tier honest. If a
role's `never-used` pool genuinely has no good independent material after a real walk,
say so as a measurement — do not pad it.

**Pluralsight:** the 404 was the right call. Find the real course URLs through
Pluralsight's own search page, not by building slugs. If they cannot be found, they do
not ship.

# Consistency — the sixteen we already hold

Eleven of the sixteen existing Help Center items say `published: UNVERIFIED`. The
ten new ones read a printed date off the page. Same publisher, two methods. Re-open the
sixteen and apply the new method: a printed date becomes the date, "updated this week"
stays `UNVERIFIED`. Log each.

# Then steps 4 and 5, as written in FIX-17

Build. Let the fingerprints go stale — that is the system working. Re-pick every stale
cell with the full card view, all four constraints, the shortlist principle. Report:

- per-cell counts before and after, and what is still thin
- the official ratio before and after, overall and per `never-used` cell
- which publisher-thin cells un-thinned, and which pools still hold zero
  non-Anthropic items
- picks that changed, with the old and new reason side by side

Validators, link check, real browser on the changed cells, deploy.

# Not yours

Still mine: the email address, nothing is `reviewed`, the subject axis, the NVDA and
High Contrast run.

# Commits

Leg 1, leg 2, the sixteen re-dated, and the re-picks — four commits. `FIX-18.md` goes
in with the round.

Finish with what surprised you and what you got wrong.
